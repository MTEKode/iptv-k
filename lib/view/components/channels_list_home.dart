import 'package:flutter/material.dart';

class ChannelsListHome extends StatelessWidget {
  const ChannelsListHome({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        decoration: BoxDecoration(
          color: Colors.blue,
        ),
        child: Column(
          children: <Widget>[
          ],
        ),
      ),
    );
  }
}